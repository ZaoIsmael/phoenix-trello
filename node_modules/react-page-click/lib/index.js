'use strict';

// Babel6 does not hack the default behaviour of ES Modules anymore, so we should export

var PageClick = require('./PageClick').default;

module.exports = PageClick;
//# sourceMappingURL=index.js.map